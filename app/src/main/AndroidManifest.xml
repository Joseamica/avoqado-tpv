<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Device Identification Permission -->
    <!-- Required for Build.getSerial() on Android 8+ to obtain hardware serial number -->
    <!-- Hardware serial persists across app reinstalls, unlike ANDROID_ID -->
    <!-- Professional POS systems (Square, Toast, Clover) use hardware serial for terminal identification -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Camera Permission (Step 4: Sale Verification) -->
    <!-- Required for capturing verification photos and scanning barcodes -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Location Permissions (Clock-in/out GPS tracking) -->
    <!-- Required for venues that enable attendance GPS verification via VenueModule config -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <!-- WiFi Permission (Network Location API) -->
    <!-- Required for WiFi scanning to improve geolocation accuracy via Google Geolocation API -->
    <!-- Cell towers alone: ~100-1000m accuracy | Cell + WiFi: ~20-50m accuracy -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Required for Phase 1 spike to test OS-level WiFi toggle on PAX -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- Bluetooth Permissions (Remote Payment Initiation - Future Feature) -->
    <!-- Required for Bluetooth Low Energy (BLE) communication with external devices -->
    <!-- Android 12+ (API 31+) uses granular permissions, older versions use legacy BLUETOOTH/BLUETOOTH_ADMIN -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />

    <!-- Foreground Service Permission (BLE Payment Server) -->
    <!-- Required for running BLE server as foreground service that persists across app updates -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <!-- Storage Permissions (Self-Update Feature) -->
    <!-- Required for downloading APK updates to external storage -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />

    <!-- Package Installation Permissions (Self-Update Feature) -->
    <!-- Required for installing APK updates via PAX SDK -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.INSTALL_PACKAGES"
        tools:ignore="ProtectedPermissions" />
    <!-- PAX-specific permission required by ISys.installApp() when permission checking is enabled -->
    <uses-permission android:name="com.pax.permission.UPDATE_APP" />

    <application
        android:name=".AvoqadoTPVApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Avoqadotpv">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="keyboard|keyboardHidden|navigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- Uncomment to make this app the default launcher (auto-launch on boot) -->
                <!-- <category android:name="android.intent.category.HOME" /> -->
                <!-- <category android:name="android.intent.category.DEFAULT" /> -->
            </intent-filter>
        </activity>

        <!-- Disable WorkManager auto-initialization (we use custom Hilt configuration) -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- FileProvider for APK installation (self-update feature) -->
        <!-- Authority uses ${applicationId} to support both sandbox and production builds -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- BLE Payment Server Foreground Service (Square/Toast Pattern) -->
        <!-- Runs BLE GATT server that persists across app updates -->
        <!-- Uses START_STICKY so Android restarts it automatically after process death -->
        <service
            android:name=".core.bluetooth.BluetoothPaymentForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- App Update Receiver - Auto-relaunch after self-update -->
        <!-- Listens for MY_PACKAGE_REPLACED to restart app after PAX SDK installs new version -->
        <receiver
            android:name=".core.receivers.AppUpdateReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
